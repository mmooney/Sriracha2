#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../ember-template-compiler/bin/compiler" "$@"
  ret=$?
else 
  node  "$basedir/../ember-template-compiler/bin/compiler" "$@"
  ret=$?
fi
exit $ret
